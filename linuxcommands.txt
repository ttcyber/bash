set a static ip on an Rpi
#ifconfig eth0 192.168.1.x  where x in a unigue number
example
#ifconfig eth0 192.168.1.10

delete a file
$rm nameofile

$delete a folder (you always don't need the -f
rm -fR nameoffolder/

copy a file
cp nameoffile  newnameoffile

colpy a folder

cp -fR nameoffolder/  /home/user/Desktop/
